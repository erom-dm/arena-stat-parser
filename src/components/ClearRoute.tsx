import React from "react";

const ClearRoute: React.FC = () => {
  return (
    <div className={"empty-route"}>
      <div>Local storage has been cleared</div>
      <div>Refresh page</div>
    </div>
  );
};

export default ClearRoute;
